# Keeps this (otherwise empty) directory in git so the Docker build's
# `COPY --from=builder /app/public ./public` step succeeds on a fresh clone.
# Drop static assets served at the site root here (favicon, etc.).
